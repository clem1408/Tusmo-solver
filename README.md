# Tusmo-solver

A solver for tusmo using **C++** and **strings**.

The directory ```/french_set/``` contain some ```.txt``` file that are related to a kind of "french word database".

To compile and run this programm do:

```
cd build/
cmake ..
make
./main
make clean
```
